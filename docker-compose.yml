version: '3.8'

services:
  exata-bank-frontend:
    image: ghcr.io/cardosolucass96/exata-bank-site-inst:latest
    
    networks:
      - CardosoNet
    
    environment:
      - NODE_ENV=production
    
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.role == manager
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 256M
      labels:
        # Habilita Traefik
        - traefik.enable=true
        - traefik.docker.network=CardosoNet
        # Domínio e roteamento
        - traefik.http.routers.exata-bank.rule=Host(`exata-bank.cardosolucas.com`)
        - traefik.http.routers.exata-bank.entrypoints=websecure
        - traefik.http.routers.exata-bank.priority=1
        - traefik.http.routers.exata-bank.tls.certresolver=letsencryptresolver
        - traefik.http.routers.exata-bank.service=exata-bank
        # Serviço (porta interna do Nginx)
        - traefik.http.services.exata-bank.loadbalancer.server.port=80
        - traefik.http.services.exata-bank.loadbalancer.passHostHeader=true
    
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

networks:
  CardosoNet:
    external: true
    name: CardosoNet